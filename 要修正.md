## ツール概要

- 通常MD / MVPc 記録の運用
- 売買履歴・資産管理の運用
- MVPc・統計の運用

## 全体

- 金額を1000z = 1kz, 1000kz=1Mz, 1000Mz=1Gz (単位は大文字小文字可)で、入力可能とする→済
- 元のツールと使い方自体は同じ想定のため、ガイドページにあるルールは踏襲したい。→済
- 縦、横の配置、並びを考えて、無駄に縦長、横長にならないように調整する。
- ソロではなく、ペア、PTで挑戦することも視野に入れる。→分配が必要になったりする。→済（取引記録の売却時、MD周回のアイテム取得数量ともにPT人数で分配されます）
- マスタにない個別追加系のものはダイアログで登録するように画面繊維して→済
- マスタ系のメニューは下の方で→済
- アイコン設定する→済

## MD進捗

## MVPカウンター

## MVPマスタ

- 初期値はどうする？→保留（内容は後で準備。追加方法は`src/features/mvp/masterData.ts`にコメントで記載済み。既存DBには反映されないので注意、既存キャラには「+ MVPを登録する」ダイアログを使う）

## ダッシュボード

## アイテムマスタ

## 取引記録

- 取引記録の販売時のアイテム検索候補は在庫一覧から抽出して。

## 収益

## 欲しいもの

- 

## MDマスタ

- MDマスタ表のアーカイブボタンの位置にLv制限の欄を持ってきて、アーカイブボタンは縦の列の最終列に設定 →済（Lv制限を1行目に移動、除外ボタン（旧アーカイブ）は専用の最終列にまとめて配置。転置表示では列見出し内のまま）

## 設定

- 全ての情報をエクスポート・インポート出来るようにして、同じ設定で複数のPCで扱える形とする。→済（全10テーブル・全フィールドが対象。エクスポート→削除→インポートの往復で新規追加項目も含めて復元されることを確認済み）
- 全データ消去機能追加、デフォルト設定から始められるようにする→済（設定タブに「全データを消去する」ボタン追加、消去後はデフォルトのMD/MVP/アイテムマスタを再セットします）

